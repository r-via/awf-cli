# Axis: utility



---

# Axis: duplication

## User

## File: `pkg/retry/retryer.go`
## Language: go

```go
package retry

import (
	"context"
	"fmt"
	"math/rand"
	"time"
)

// Config holds retry behavior configuration.
type Config struct {
	MaxAttempts        int           // max retry attempts (1 = no retry)
	InitialDelay       time.Duration // base delay between attempts
	MaxDelay           time.Duration // maximum delay cap
	Strategy           Strategy      // backoff strategy
	Multiplier         float64       // multiplier for exponential backoff
	Jitter             float64       // randomness factor [0.0, 1.0]
	RetryableExitCodes []int         // exit codes that trigger retry (empty = any non-zero)
}

// Logger defines the logging interface for retry operations.
type Logger interface {
	Debug(msg string, keysAndValues ...any)
	Info(msg string, keysAndValues ...any)
}

// Retryer handles retry logic with configurable backoff.
type Retryer struct {
	config Config
	logger Logger
	rng    *rand.Rand
}

// NewRetryer uses a seeded random source for jitter; pass a fixed seed for deterministic tests.
func NewRetryer(config *Config, logger Logger, seed int64) *Retryer {
	return &Retryer{
		config: *config,
		logger: logger,
		rng:    rand.New(rand.NewSource(seed)),
	}
}

// ShouldRetry returns true if the operation should be retried.
func (r *Retryer) ShouldRetry(exitCode, attempt int) bool {
	if exitCode == 0 {
		return false
	}

	if attempt >= r.config.MaxAttempts {
		return false
	}

	return r.IsRetryableExitCode(exitCode)
}

// NextDelay returns the delay to wait before the next attempt.
func (r *Retryer) NextDelay(attempt int) time.Duration {
	delay := CalculateDelay(
		r.config.Strategy,
		attempt,
		r.config.InitialDelay,
		r.config.MaxDelay,
		r.config.Multiplier,
	)

	return ApplyJitter(delay, r.config.Jitter, r.rng)
}

// Wait sleeps for the calculated delay, respecting context cancellation.
func (r *Retryer) Wait(ctx context.Context, attempt int) error {
	delay := r.NextDelay(attempt)

	if r.logger != nil {
		r.logger.Debug(
			"waiting before retry",
			"attempt", attempt,
			"delay", delay.String(),
		)
	}

	timer := time.NewTimer(delay)
	defer timer.Stop()

	select {
	case <-ctx.Done():
		return fmt.Errorf("retry wait interrupted: %w", ctx.Err())
	case <-timer.C:
		return nil
	}
}

// IsRetryableExitCode checks if the given exit code should trigger a retry.
func (r *Retryer) IsRetryableExitCode(exitCode int) bool {
	if exitCode == 0 {
		return false
	}

	if len(r.config.RetryableExitCodes) == 0 {
		return true
	}

	for _, code := range r.config.RetryableExitCodes {
		if code == exitCode {
			return true
		}
	}

	return false
}

```

## Symbols to evaluate

- export class Config (L11–L19)
- export type Logger (L22–L25)
- export class Retryer (L28–L32)
- export function NewRetryer (L35–L41)
- export method ShouldRetry (L44–L54)
- export method NextDelay (L57–L67)
- export method Wait (L70–L90)
- export method IsRetryableExitCode (L93–L109)

## RAG — Semantic Duplication

### NewRetryer (L35–L41)
No similar functions found. Mark UNIQUE.

### ShouldRetry (L44–L54)
Similar functions found:
- **IsRetryableExitCode** in `pkg/retry/retryer.go` (score: 0.722)
  Signature: func (r *Retryer) IsRetryableExitCode(exitCode int) bool {
  Complexity: 1/5
  Source:
  ```go
  	return r.IsRetryableExitCode(exitCode)
  }
  
  // NextDelay returns the delay to wait before the next attempt.
  func (r *Retryer) NextDelay(attempt int) time.Duration {
  	delay := CalculateDelay(
  		r.config.Strategy,
  		attempt,
  		r.config.InitialDelay,
  		r.config.MaxDelay,
  		r.config.Multiplier,
  	)
  
  	return ApplyJitter(delay, r.config.Jitter, r.rng)
  }
  
  // Wait sleeps for the calculated delay, respecting context cancellation.
  func (r *Retryer) Wait(ctx context.Context, attempt int) error {
  	delay := r.NextDelay(attempt)
  
  	if r.logger != nil {
  		r.logger.Debug(
  			"waiting before retry",
  			"attempt", attempt,
  			"delay", delay.String(),
  		)
  	}
  
  	timer := time.NewTimer(delay)
  	defer timer.Stop()
  
  	select {
  	case <-ctx.Done():
  		return fmt.Errorf("retry wait interrupted: %w", ctx.Err())
  	case <-timer.C:
  		return nil
  	}
  }
  
  // IsRetryableExitCode checks if the given exit code should trigger a retry.
  func (r *Retryer) IsRetryableExitCode(exitCode int) bool {
  	if exitCode == 0 {
  		return false
  	}
  
  	if len(r.config.RetryableExitCodes) == 0 {
  		return true
  	}
  
  	for _, code := range r.config.RetryableExitCodes {
  ```

### NextDelay (L57–L67)
No similar functions found. Mark UNIQUE.

### Wait (L70–L90)
No similar functions found. Mark UNIQUE.

### IsRetryableExitCode (L93–L109)
Similar functions found:
- **ShouldRetry** in `pkg/retry/retryer.go` (score: 0.722)
  Signature: func (r *Retryer) ShouldRetry(exitCode, attempt int) bool {
  Complexity: 1/5
  Calls: IsRetryableExitCode
  Source:
  ```go
  func (r *Retryer) ShouldRetry(exitCode, attempt int) bool {
  	if exitCode == 0 {
  		return false
  	}
  
  	if attempt >= r.config.MaxAttempts {
  		return false
  	}
  
  	return r.IsRetryableExitCode(exitCode)
  }
  
  // NextDelay returns the delay to wait before the next attempt.
  func (r *Retryer) NextDelay(attempt int) time.Duration {
  	delay := CalculateDelay(
  		r.config.Strategy,
  		attempt,
  		r.config.InitialDelay,
  		r.config.MaxDelay,
  		r.config.Multiplier,
  	)
  
  	return ApplyJitter(delay, r.config.Jitter, r.rng)
  }
  
  // Wait sleeps for the calculated delay, respecting context cancellation.
  func (r *Retryer) Wait(ctx context.Context, attempt int) error {
  	delay := r.NextDelay(attempt)
  
  	if r.logger != nil {
  		r.logger.Debug(
  			"waiting before retry",
  			"attempt", attempt,
  			"delay", delay.String(),
  		)
  	}
  
  	timer := time.NewTimer(delay)
  	defer timer.Stop()
  
  	select {
  	case <-ctx.Done():
  		return fmt.Errorf("retry wait interrupted: %w", ctx.Err())
  	case <-timer.C:
  		return nil
  	}
  }
  
  // IsRetryableExitCode checks if the given exit code should trigger a retry.
  func (r *Retryer) IsRetryableExitCode(exitCode int) bool {
  ```
- **isRetryableStatus** in `internal/infrastructure/http/provider.go` (score: 0.721)
  Signature: func isRetryableStatus(statusCode int, retryableCodes []int) bool {
  Complexity: 1/5
  Source:
  ```go
  	if isRetryableStatus(resp.StatusCode, retryableStatusCodes) {
  		return &pluginmodel.OperationResult{
  			Success: false,
  			Outputs: buildOutputs(resp),
  			Error:   fmt.Sprintf("http.request: retryable status code %d", resp.StatusCode),
  		}, nil
  	}
  
  	// Success - return response data
  	return &pluginmodel.OperationResult{
  		Success: true,
  		Outputs: buildOutputs(resp),
  		Error:   "",
  	}, nil
  }
  
  // validateRequiredInputs validates URL and method inputs
  func validateRequiredInputs(inputs map[string]any) error {
  	url, ok := inputs["url"].(string)
  	if !ok || url == "" {
  		return fmt.Errorf("http.request: url is required and must be a non-empty string")
  	}
  
  	if !strings.HasPrefix(url, "http://") && !strings.HasPrefix(url, "https://") {
  		return fmt.Errorf("http.request: url must start with http:// or https://")
  	}
  
  	methodRaw, ok := inputs["method"].(string)
  	if !ok || methodRaw == "" {
  		return fmt.Errorf("http.request: method is required and must be a non-empty string")
  	}
  
  	method := strings.ToUpper(methodRaw)
  	validMethods := map[string]bool{
  		http.MethodGet:    true,
  		http.MethodPost:   true,
  		http.MethodPut:    true,
  		http.MethodDelete: true,
  	}
  	if !validMethods[method] {
  		return fmt.Errorf("http.request: method must be one of: GET, POST, PUT, DELETE")
  	}
  
  	return nil
  }
  
  // extractHeaders extracts and converts headers from inputs
  func extractHeaders(inputs map[string]any) map[string]string {
  	headersRaw, exists := inputs["headers"]
  	if !exists || headersRaw == nil {
  ```

Evaluate the duplication of each symbol and output the JSON.

## System (init)

**Model:** claude-haiku-4-5-20251001
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

```json
{
  "symbols": [
    {
      "name": "Config",
      "line_start": 11,
      "line_end": 19,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar types found in RAG search"
    },
    {
      "name": "Logger",
      "line_start": 22,
      "line_end": 25,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar interfaces in RAG search"
    },
    {
      "name": "Retryer",
      "line_start": 28,
      "line_end": 32,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar struct types found"
    },
    {
      "name": "NewRetryer",
      "line_start": 35,
      "line_end": 41,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar constructors in RAG search"
    },
    {
      "name": "ShouldRetry",
      "line_start": 44,
      "line_end": 54,
      "duplication": "UNIQUE",
      "confidence": 88,
      "detail": "Score 0.722 with IsRetryableExitCode. Different contract: ShouldRetry checks attempt limit and delegates; IsRetryableExitCode validates code membership. Non-interchangeable despite structural similarity."
    },
    {
      "name": "NextDelay",
      "line_start": 57,
      "line_end": 67,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar delay calculation functions"
    },
    {
      "name": "Wait",
      "line_start": 70,
      "line_end": 90,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar context-aware wait functions"
    },
    {
      "name": "IsRetryableExitCode",
      "line_start": 93,
      "line_end": 109,
      "duplication": "UNIQUE",
      "confidence": 82,
      "detail": "Score 0.722 vs ShouldRetry, 0.721 vs isRetryableStatus. Semantically distinct: validates exit code membership in configured list; different contexts prevent interchangeability."
    }
  ]
}
```

## rate_limit_event

## Result (success)

**Cost:** $0.0308 | **Duration:** 28.2s


---

# Axis: correction

## User

## File: `pkg/retry/retryer.go`
## Language: go

```go
package retry

import (
	"context"
	"fmt"
	"math/rand"
	"time"
)

// Config holds retry behavior configuration.
type Config struct {
	MaxAttempts        int           // max retry attempts (1 = no retry)
	InitialDelay       time.Duration // base delay between attempts
	MaxDelay           time.Duration // maximum delay cap
	Strategy           Strategy      // backoff strategy
	Multiplier         float64       // multiplier for exponential backoff
	Jitter             float64       // randomness factor [0.0, 1.0]
	RetryableExitCodes []int         // exit codes that trigger retry (empty = any non-zero)
}

// Logger defines the logging interface for retry operations.
type Logger interface {
	Debug(msg string, keysAndValues ...any)
	Info(msg string, keysAndValues ...any)
}

// Retryer handles retry logic with configurable backoff.
type Retryer struct {
	config Config
	logger Logger
	rng    *rand.Rand
}

// NewRetryer uses a seeded random source for jitter; pass a fixed seed for deterministic tests.
func NewRetryer(config *Config, logger Logger, seed int64) *Retryer {
	return &Retryer{
		config: *config,
		logger: logger,
		rng:    rand.New(rand.NewSource(seed)),
	}
}

// ShouldRetry returns true if the operation should be retried.
func (r *Retryer) ShouldRetry(exitCode, attempt int) bool {
	if exitCode == 0 {
		return false
	}

	if attempt >= r.config.MaxAttempts {
		return false
	}

	return r.IsRetryableExitCode(exitCode)
}

// NextDelay returns the delay to wait before the next attempt.
func (r *Retryer) NextDelay(attempt int) time.Duration {
	delay := CalculateDelay(
		r.config.Strategy,
		attempt,
		r.config.InitialDelay,
		r.config.MaxDelay,
		r.config.Multiplier,
	)

	return ApplyJitter(delay, r.config.Jitter, r.rng)
}

// Wait sleeps for the calculated delay, respecting context cancellation.
func (r *Retryer) Wait(ctx context.Context, attempt int) error {
	delay := r.NextDelay(attempt)

	if r.logger != nil {
		r.logger.Debug(
			"waiting before retry",
			"attempt", attempt,
			"delay", delay.String(),
		)
	}

	timer := time.NewTimer(delay)
	defer timer.Stop()

	select {
	case <-ctx.Done():
		return fmt.Errorf("retry wait interrupted: %w", ctx.Err())
	case <-timer.C:
		return nil
	}
}

// IsRetryableExitCode checks if the given exit code should trigger a retry.
func (r *Retryer) IsRetryableExitCode(exitCode int) bool {
	if exitCode == 0 {
		return false
	}

	if len(r.config.RetryableExitCodes) == 0 {
		return true
	}

	for _, code := range r.config.RetryableExitCodes {
		if code == exitCode {
			return true
		}
	}

	return false
}

```

## Symbols to evaluate

- export class Config (L11–L19)
- export type Logger (L22–L25)
- export class Retryer (L28–L32)
- export function NewRetryer (L35–L41)
- export method ShouldRetry (L44–L54)
- export method NextDelay (L57–L67)
- export method Wait (L70–L90)
- export method IsRetryableExitCode (L93–L109)

## REFERENCE DOCUMENTATION (AUTHORITATIVE — human-authored or human-validated)

Use the following as ground truth. If the code you are reviewing contradicts an invariant, convention, or non-goal stated here, flag it as a `doc_divergence` finding (not as a regular bug).

### Internal Reference Digest (source: reference-digest)

---
status: evaluated
source: reference-digest
---
## Reference index

_No reference documents found._

## Inline doc-comments

_No inline doc-comments found._

_(truncated to 28 tokens; dropped 1985 entries)_


Identify any bugs, logic errors, or correctness issues and output the JSON.

## System (init)

**Model:** claude-sonnet-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{
  "symbols": [
    {
      "name": "Config",
      "line_start": 11,
      "line_end": 19,
      "correction": "OK",
      "confidence": 95,
      "detail": "Struct definition; no logic to evaluate."
    },
    {
      "name": "Logger",
      "line_start": 22,
      "line_end": 25,
      "correction": "OK",
      "confidence": 95,
      "detail": "Interface definition; no correctness issues."
    },
    {
      "name": "Retryer",
      "line_start": 28,
      "line_end": 32,
      "correction": "NEEDS_FIX",
      "confidence": 70,
      "detail": "rng *rand.Rand is not safe for concurrent use; no mutex protects it.",
      "findings": [
        {
          "line_start": 31,
          "line_end": 31,
          "detail": "math/rand.Rand is explicitly documented as not safe for concurrent use. If the same Retryer handles concurrent retry loops (a common pattern), simultaneous calls to NextDelay/Wait race on rng. Fix: add a sync.Mutex field to Retryer and lock around rng access, or replace rng with a sync-safe source."
        }
      ]
    },
    {
      "name": "NewRetryer",
      "line_start": 35,
      "line_end": 41,
      "correction": "OK",
      "confidence": 90,
      "detail": "Initialization is correct; race condition ownership belongs to the struct definition."
    },
    {
      "name": "ShouldRetry",
      "line_start": 44,
      "line_end": 54,
      "correction": "OK",
      "confidence": 85,
      "detail": "Logic is correct assuming callers pass 1-indexed attempt counts consistent with MaxAttempts semantics (MaxAttempts=1 → no retry when attempt=1)."
    },
    {
      "name": "NextDelay",
      "line_start": 57,
      "line_end": 67,
      "correction": "OK",
      "confidence": 85,
      "detail": "Correctly delegates to CalculateDelay and ApplyJitter; no visible logic errors in the delegation chain."
    },
    {
      "name": "Wait",
      "line_start": 70,
      "line_end": 90,
      "correction": "OK",
      "confidence": 95,
      "detail": "Context cancellation and timer teardown via defer timer.Stop() are handled correctly."
    },
    {
      "name": "IsRetryableExitCode",
      "line_start": 93,
      "line_end": 109,
      "correction": "OK",
      "confidence": 95,
      "detail": "Correctly short-circuits on zero, allows any non-zero when allowlist is empty, and performs linear scan of explicit allowlist."
    }
  ],
  "actions": [
    {
      "description": "Guard rng with a sync.Mutex in Retryer (or switch to a locked rand source) to prevent data races when Wait/NextDelay are called concurrently on the same instance.",
      "severity": "MAJOR",
      "line": 31
    }
  ],
  "doc_divergences": []
}

## rate_limit_event

## Result (success)

**Cost:** $0.2769 | **Duration:** 71.4s

